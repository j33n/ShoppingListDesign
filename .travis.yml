language: python
python:
  - "3.5"

# command to install dependencies
install:
  - pip install -r requirements.txt


# command to run tests.py
script: 
  - nosetests -v
  - nosetests --with-coverage --cover-inclusive --cover-package=app/tests
  - coveralls

after_sucess:
  - "coveralls"
